# Copy to .env.local for local development.
# Base URL of the Django backend. Exposed to the browser bundle — never put
# secrets in NEXT_PUBLIC_* variables. Baked in at build time for production.
NEXT_PUBLIC_API_URL=http://localhost:8000
